import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); //this create react virtual dom like real dom having  div id root in html
root.render(
    <App />
);


